--- src/t/unit-tests/clar/clar/fs.h.orig
+++ src/t/unit-tests/clar/clar/fs.h
@@ -317,7 +317,7 @@
 # include <sys/sendfile.h>
 #endif
 
-#if defined(__APPLE__)
+#if USE_FCOPYFILE && defined(__APPLE__)
 # include <copyfile.h>
 #endif
 
